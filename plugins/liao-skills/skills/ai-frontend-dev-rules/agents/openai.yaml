interface:
  display_name: "AI Frontend Dev Rules"
  short_description: "Frontend architecture rules for AI-assisted development."
  default_prompt: "Use $ai-frontend-dev-rules to keep frontend module boundaries, API integration, state/data ownership, and delivery reporting aligned with the project."

policy:
  allow_implicit_invocation: true
