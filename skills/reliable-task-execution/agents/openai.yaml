interface:
  display_name: "Reliable Task Execution"
  short_description: "Refine, authorize, execute, and verify reliably"
  default_prompt: "Use $reliable-task-execution to start with the lightest sufficient workflow, recommend Full Brainstorm or Strict Grill when warranted, confirm authorization, and verify the result."

policy:
  allow_implicit_invocation: true
