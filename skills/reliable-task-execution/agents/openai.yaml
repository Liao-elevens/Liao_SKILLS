interface:
  display_name: "Reliable Task Execution"
  short_description: "Refine, authorize, execute, and verify reliably"
  default_prompt: "Use $reliable-task-execution to refine material uncertainty, choose the minimum sufficient workflow, confirm authorization, and verify the result."

policy:
  allow_implicit_invocation: true
