interface:
  display_name: "Reliable Task Execution"
  short_description: "Plan, authorize, execute, and verify reliably"
  default_prompt: "Use $reliable-task-execution to choose the minimum sufficient workflow, align the plan, and verify the result."

policy:
  allow_implicit_invocation: true
