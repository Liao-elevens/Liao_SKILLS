interface:
  display_name: "Reliable Task Execution"
  short_description: "Analyze first; edit only on explicit instruction"
  default_prompt: "Use $reliable-task-execution with the lightest sufficient workflow; stay read-only unless I explicitly instruct changes, then complete and verify those authorized changes."

policy:
  allow_implicit_invocation: true
